[package]
name = "hola_paso"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen-rt = { version = "0.41.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "anvil:paso"

[package.metadata.component.dependencies]

# Standalone (fuera del workspace del core): el componente es un ejemplo
# independiente, igual que lo sería el proyecto de un usuario externo.
[workspace]
